# Bloom API (production)
NEXT_PUBLIC_API_URL=https://api.bloomit.ai
NEXT_PUBLIC_APP_URL=https://dev.bloomit.ai

# GitHub OAuth (optional — only needed if running auth locally)
# Get your own at https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
